import styled from "styled-components";

const Main = styled.main`
  width: 100%;
  height: auto;
  display: flex;
  flex-wrap: wrap;
  background-color: var(--lg-gray);
  z-index: 0;
`;

export default Main;
